{
  "name": "djinn",
  "displayName": "Djinn",
  "description": "Capture todos in your repo and sync them to Azure DevOps, GitHub, or GitLab. Delegate work to Claude Code or Copilot agents.",
  "version": "0.4.0",
  "publisher": "moizmh",
  "engines": {
    "vscode": "^1.93.0"
  },
  "categories": ["Other"],
  "main": "./out/extension.js",
  "activationEvents": [
    "onView:djinn.todoTree",
    "workspaceContains:.todos/todos.json",
    "workspaceContains:.ado/todos.json"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "djinn",
          "title": "Djinn",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "djinn": [
        {
          "id": "djinn.todoTree",
          "name": "Todos"
        }
      ]
    },
    "commands": [
      { "command": "djinn.add", "title": "Djinn: Add Todo", "icon": "$(add)" },
      { "command": "djinn.sync", "title": "Djinn: Sync", "icon": "$(cloud-upload)" },
      { "command": "djinn.refresh", "title": "Djinn: Refresh", "icon": "$(refresh)" },
      { "command": "djinn.edit", "title": "Edit", "icon": "$(edit)" },
      { "command": "djinn.delete", "title": "Delete", "icon": "$(trash)" },
      { "command": "djinn.runAgent", "title": "Run with Agent…", "icon": "$(play)" },
      { "command": "djinn.openMetrics", "title": "Djinn: Open Metrics Dashboard", "icon": "$(graph)" },
      { "command": "djinn.configure", "title": "Djinn: Configure Provider" }
    ],
    "menus": {
      "view/title": [
        { "command": "djinn.add", "when": "view == djinn.todoTree", "group": "navigation@1" },
        { "command": "djinn.sync", "when": "view == djinn.todoTree", "group": "navigation@2" },
        { "command": "djinn.openMetrics", "when": "view == djinn.todoTree", "group": "navigation@3" },
        { "command": "djinn.refresh", "when": "view == djinn.todoTree", "group": "navigation@4" }
      ],
      "view/item/context": [
        { "command": "djinn.runAgent", "when": "view == djinn.todoTree && viewItem == todo", "group": "inline@1" },
        { "command": "djinn.delete", "when": "view == djinn.todoTree && viewItem == todo", "group": "inline@2" }
      ]
    },
    "configuration": {
      "title": "Djinn",
      "properties": {
        "djinn.defaultAgent": {
          "type": "string",
          "enum": ["claude-code", "copilot", "open-issue"],
          "default": "claude-code",
          "description": "Default agent to run when 'Run with Agent' has no explicit choice."
        },
        "djinn.claudeCodeCommand": {
          "type": "string",
          "default": "claude",
          "description": "Command used to invoke Claude Code CLI."
        },
        "djinn.claudeCode.model": {
          "type": "string",
          "enum": ["sonnet", "opus", "haiku"],
          "default": "sonnet",
          "description": "Default model for Claude Code agent runs."
        },
        "djinn.claudeCode.reasoning": {
          "type": "string",
          "enum": ["none", "think", "think-hard", "ultrathink"],
          "default": "none",
          "description": "Default reasoning intensity hint appended to Claude Code prompts."
        },
        "djinn.claudeCode.approvals": {
          "type": "string",
          "enum": ["default", "acceptEdits", "plan", "bypassPermissions"],
          "default": "default",
          "description": "Default --permission-mode for Claude Code."
        },
        "djinn.copilot.mode": {
          "type": "string",
          "enum": ["agent", "edit", "ask"],
          "default": "agent",
          "description": "Default Copilot Chat mode slash-command."
        },
        "djinn.copilot.model": {
          "type": "string",
          "default": "",
          "description": "Optional model hint included in the Copilot prompt."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "dependencies": {
    "@gitbeaker/rest": "^40.5.1",
    "@octokit/rest": "^21.0.2",
    "azure-devops-node-api": "^14.0.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/uuid": "^9.0.8",
    "@types/vscode": "^1.93.0",
    "@vscode/vsce": "^3.2.1",
    "typescript": "^5.4.5"
  }
}
